import 'package:flutter/material.dart';

class Like extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(child: Text("Like",style: TextStyle(fontSize: 20.0,color: Colors.white),));
  }
}
